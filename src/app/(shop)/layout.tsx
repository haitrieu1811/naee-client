import { ReactNode } from "react";

const ShopLayout = ({ children }: { children: ReactNode }) => {
  return (
    <div>
      Shop layout
      {children}
    </div>
  );
};

export default ShopLayout;
